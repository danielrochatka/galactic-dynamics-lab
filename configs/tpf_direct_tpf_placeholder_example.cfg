# Example: select the canonical direct_tpf dynamics branch.
# Requires tpf_vdsg_coupling = 0 until VDSG is defined for the direct path.
# Does NOT require tpfcore_enable_provisional_readout for dynamics.
# Effective mode/IC resolution is emitted to resolved_scenario.txt/json.

physics_package = TPFCore
tpf_dynamics_mode = direct_tpf
tpfcore_enable_provisional_readout = false
tpf_vdsg_coupling = 0

# Example benchmark mode (defaults for dt/n_steps/softening resolve by mode):
# simulation_mode = earth_moon_benchmark
