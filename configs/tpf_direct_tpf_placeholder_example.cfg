# Example: select the future direct TPF dynamics branch (plumbing only).
# Requires tpf_vdsg_coupling = 0 until VDSG is defined for the direct path.
# Does NOT require tpfcore_enable_provisional_readout for dynamics.
#
# Expected today: first integrator step throws:
#   direct_tpf selected but compute_direct_tpf_accelerations is not implemented yet
#
# active_dynamics_branch: TPF_direct

physics_package = TPFCore
tpf_dynamics_mode = direct_tpf
tpfcore_enable_provisional_readout = false
tpf_vdsg_coupling = 0
