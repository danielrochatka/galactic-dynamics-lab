# Newtonian package defaults
# This package uses engine-level settings only (softening, bh_mass, enable_star_star_gravity).
# No package-specific keys. This file is optional; kept for consistency with the package config system.
